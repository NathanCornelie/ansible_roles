---
all:
  children:
    servers:
      hosts:
        bsd:
          ansible_host: 172.30.12.7
          ansible_become_method: doas

        mainServ:
          ansible_host: localhost
          ansible_become_method: sudo
